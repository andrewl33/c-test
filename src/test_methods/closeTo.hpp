#ifndef CLOSETO_HPP
#define CLOSETO_HPP

#include <string>

// checks to see that the values close to each other 

// does not have longs for now, and check with double or more

bool closeTo(int, int, double);
bool closeTo(float, float, double);
bool closeTo(double, double, double);

#endif