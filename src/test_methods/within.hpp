#ifndef WITHIN_HPP
#define WITHIN_HPP

bool within(int[2], double);
bool within(long int[2], long double);
bool within(float[2], double);
bool within(double[2], double);
bool within(long double[2], long double);

#endif